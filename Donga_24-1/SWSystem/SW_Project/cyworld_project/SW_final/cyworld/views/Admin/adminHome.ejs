<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>관리자 페이지</title>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="/stylesheets/font.css">
    <link rel="stylesheet" href="/stylesheets/adminHome.css">
    <script src="https://kit.fontawesome.com/2e4fe0201a.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <div class="container">
      <div class="bookcover">
        <div class="logo-title-box">
          <div class="logo">
            <div class="logo-text">관리자 페이지</div>
          </div>
        </div>
        <div class="admin-content">
          <div class="admin-section">
            <h2>총 회원 수</h2>
            <div class="admin-box"><%= totalMemberNum %>명</div>
          </div>
          <div class="admin-section">
            <h2>회원 정보 테이블</h2>
            <div class="admin-box">
              <div class="member-list-title">
                <div class="member-items">아이디</div>
                <div class="member-items">비밀번호</div>
                <div class="member-items">닉네임</div>
                <div class="member-items">생성 날짜</div>
              </div>
              <% if (memberListDto && memberListDto.length > 0) { %>
                <% memberListDto.forEach(function(member) { %>
                  <div class="member-list-container">
                    <div class="member-items"><%= member.getId() %></div>
                    <div class="member-items"><%= member.getPassword() %></div>
                    <div class="member-items"><%= member.getNickname() %></div>
                    <div class="member-items"><%= member.getDate() %></div>
                  </div>
                  <% }); %>
              <% } else { %>
                <div class="no-image-msg" style="text-align: center; color: #666;">등록된 회원이 없습니다.</div>
              <% } %>

            </div>
          </div>
          <div class="admin-section">
            <h2>신고 접수 내역</h2>
            <div class="admin-box">
              <div class="member-list-title">
                <div class="member-items">신고한 id</div>
                <div class="member-items">신고 당한 id</div>
                <div class="member-items">신고 사유</div>
                <div class="member-items">신고 날짜</div>
              </div>
              <% if (reportListDto && reportListDto.length > 0) { %>
                <% reportListDto.forEach(function(report) { %>
                  <div class="member-list-container">
                    <div class="member-items"><%= report.getReporterId() %></div>
                    <div class="member-items"><%= report.getReprotedId() %></div>
                    <div class="member-items"><%= report.getReason() %></div>
                    <div class="member-items"><%= report.getDate() %></div>
                  </div>
                  <% }); %>
              <% } else { %>
                <div class="no-image-msg" style="text-align: center; color: #666;">등록된 회원이 없습니다.</div>
              <% } %>

            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="/functions/Admin/f_adminHome.js"></script>
  </body>
</html>
